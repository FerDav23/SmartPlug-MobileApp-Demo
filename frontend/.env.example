# Copy this file to .env and adjust as needed.
# Do not commit .env (it may contain secrets).

# -----------------------------------------------------------------------------
# Demo mode
# -----------------------------------------------------------------------------
# Set to "true" to run the app in demo mode: no backend, mock data, no auth.
# Omit or set to anything else for normal (production) mode.
#
# EXPO_PUBLIC_DEMO_MODE=true
